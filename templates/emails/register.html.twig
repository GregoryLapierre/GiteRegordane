<h1>Activer votre compte</h1>
<p>Bonjour {{ user.firstname }} {{ user.name }}.</p>
<p>Votre inscription sur le site du Gîte de la Régordane est à valider en cliquant sur le lien ci-dessous :</p>
<a href="{{ absolute_url(path('verify_user', {token: token})) }}">{{ absolute_url(path('verify_user', {token: token})) }}</a>
<p>Ce lien expirera dans 3 heures.</p>
<p>Merci</p>